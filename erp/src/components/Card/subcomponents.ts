import styled from 'styled-components'
import FlexWrapper from 'components/FlexWrapper'

export const TextWrapper = styled.div`
  text-align: end;
`

export const CardFooter = styled(FlexWrapper)`
  padding: 12px;
  align-items: center;
`
